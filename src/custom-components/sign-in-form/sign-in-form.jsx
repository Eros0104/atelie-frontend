import React from 'react';
import { Container, Input, Button, H1 } from '@components';

const SignUpForm = () => (
  <Container>
    <H1 white>Login</H1>
    <Input/>
    <Button>Enviar</Button>
  </Container>
);

export default SignUpForm;
